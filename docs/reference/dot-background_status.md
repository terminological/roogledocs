# Async function status

Print any asynchronous java function calls that are in progress,
complete or cancelled. The function returns a thread id which may be
used to retrieve the thread itself

## Usage

``` r
.background_status()
```

## Value

a maybe empty dataframe with \`id\` and \`status\` columns
